import { DisableDoneButtonDirective } from './disable-done-button.directive';

describe('DisableDoneButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDoneButtonDirective();
    expect(directive).toBeTruthy();
  });
});
